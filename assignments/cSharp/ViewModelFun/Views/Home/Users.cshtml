@{
    ViewData["Title"] = "Home Page";
}
@model List<User>;

<div class="text-center">
    <h1>Here are some Users!</h1>
    @{
        foreach (User name in Model)
        {
            <h3>@name.Name</h3>
        }
    }
</div>