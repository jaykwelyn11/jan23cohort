@{
    ViewData["Title"] = "Numbers";
}
@model int[];

<div class="text-center">
    @{
        foreach (int number in Model)
        {
            <h3>@number</h3>
        }
        for (int i = 0; i < Model.Length; i++)
        {
            <h5>@Model[i]</h5>
        }
    }
</div>